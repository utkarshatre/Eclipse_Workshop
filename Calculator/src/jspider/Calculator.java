package jspider;

public interface Calculator {
	int add(int x, int y);  //all the method are automatic abstract
	int sub(int x, int y);
	int mul(int x, int y);
	int div(int x, int y);
	String displayErrorMessage();

}
// all he above methods are automatic pblic and abstract
