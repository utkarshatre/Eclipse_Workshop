package com.createNode;

public class Node {
	int data;
	Node next;
	//1)if no linkedlist present so create new linkedlist 2) if present so attacch the node at last
	Node(int data){
		this.data=data;
		
	} 

}
